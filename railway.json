{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "cd backend && npm install"
  },
  "deploy": {
    "startCommand": "node backend/server/index.js",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
