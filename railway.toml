[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.nixpacksVariables]
NIXPACKS_NODE_VERSION = "22"

[deploy]
startCommand = "node server/index.js"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
