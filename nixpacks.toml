# XTOX Backend — force Node.js 24 on Railway
[phases.setup]
nixPkgs = ["nodejs-24_x"]

[phases.install]
cmds = ["cd backend && npm install --legacy-peer-deps"]

[start]
cmd = "cd backend && node server/index.js"
