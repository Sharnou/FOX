'use client';
// VoiceCall component — WebRTC peer connection wrapper
// Full implementation is in /chat/page.js
export default function VoiceCall({ socket, targetId, userId }) {
  return <div className="text-sm text-gray-400">VoiceCall: embedded in ChatPage via WebRTC</div>;
}
