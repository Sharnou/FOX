'use client';
// ChatBox component — embedded chat widget
export default function ChatBox({ targetId, adId }) {
  return <div className="p-4 border rounded-xl bg-white shadow text-sm text-gray-500">Chat component — see /chat page for full implementation</div>;
}
